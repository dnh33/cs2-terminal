// Phase 4.5 Plan 4 — single source of truth for the displayed model id.
// Synced with worker/src/index.ts DEFAULT_MODEL ('anthropic/claude-sonnet-4.5').
// Phase 5+ when worker exposes a /config endpoint, this becomes a fetched value.
export const DISPLAYED_MODEL_ID = 'claude-sonnet-4.5'
